---
config:
  target: https://v2dlil9im2.execute-api.us-east-1.amazonaws.com/dev/
  phases:
    - duration: 120
      arrivalRate: 20
      name: Sustained peak load
scenarios:
  - flow:
      - get:
          url: "/lambda"
