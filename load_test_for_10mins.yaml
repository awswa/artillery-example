config:
  target: {Replace this with your URL}
  phases:
    - duration: 600
      arrivalRate: 10
      name: CodeGuru Profiler test
scenarios:
    - flow:
        - get:
            url: "/lambda?rank={{ $randomNumber(1, 100) }}"
